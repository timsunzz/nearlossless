export type PrincipleSection = {
  id: string;
  title: string;
  paragraphs: string[];
  bullets?: string[];
};

export const PRINCIPLES_INTRO =
  "近原把「接近无损」落成一套可核对的工程选择：默认不降分辨率、不抽帧、不用固定低码率去硬砍体积，而是用 FFmpeg 的 CRF（恒定质量）按人眼敏感度重新分配比特。有本机 FFmpeg 时走服务器编码，更快，并支持 H.265 与 AV1；没有则自动改用浏览器里的 ffmpeg.wasm，视频不离开你的设备。";

export const PRINCIPLES_SECTIONS: PrincipleSection[] = [
  {
    id: "what-near-lossless-means",
    title: "接近无损到底指什么",
    paragraphs: [
      "视频压缩里有两种「无损」，经常被混用。数学无损要求解码后每一个像素都与编码前完全一致，常见于 FFV1、HuffYUV。这类文件通常仍然很大，不适合日常分享。",
      "近原做的是视觉接近无损：允许极少量、通常不可察觉的量化误差，换取明显更小的文件。x264 社区长期把 CRF 17–18 当作「透明」或「存档级有损」的经验区间；近原的「近无损」档把 H.264 放到 CRF 16，H.265 / AV1 则略高一档，因为这两套编码器在相同数字上通常更「狠」。",
      "因此近原不会声称完全无损。它承诺的是：在默认路径下，尽量不改变你看见的品质，同时尽量去掉源文件里用不到的码率。",
    ],
  },
  {
    id: "two-paths",
    title: "两条编码路径：服务器或浏览器",
    paragraphs: [
      "打开页面时，近原会探测这台主机有没有可用的 FFmpeg，以及能否使用 libx264、libx265、libsvtav1。探测结果决定默认走哪条路，也会限制 AV1 是否可选。",
    ],
    bullets: [
      "服务器路径：浏览器把文件 POST 到本机任务接口，FFmpeg 在临时目录编码，进度按已处理时长回传，完成后提供下载。临时文件最多保留一小时。",
      "浏览器路径：用 ffmpeg.wasm 在本地解码和编码，适合没有系统 FFmpeg 的部署（例如默认的 Vercel）。视频不会上传，但更吃内存，速度也更慢，AV1 不可用。",
      "两条路径共用同一套 CRF、像素格式和封装参数，只是速度预设在浏览器里会更快一档，以免把标签页拖死。",
    ],
  },
  {
    id: "pipeline",
    title: "一次压缩会经过哪些步骤",
    paragraphs: [
      "无论走哪条路径，处理顺序是一样的：先读元数据，再按你选的画质、编码和分辨率拼出 FFmpeg 参数，最后封装成便于播放的 MP4。",
    ],
    bullets: [
      "探测：读取时长、宽高、帧率、视频/音频编码和是否有音轨。",
      "选型：H.264（libx264）、H.265（libx265，标签 hvc1）或 AV1（libsvtav1，仅服务器）。",
      "视频：CRF 控制质量，preset 控制压缩效率，像素格式固定 yuv420p。",
      "缩放：仅在你选择 1080p / 720p 上限且源画面更大时，按比例缩小并保证宽高为偶数。",
      "音频：能安全复制则 copy；否则按当前画质档转 AAC。",
      "封装：MP4 +faststart，把 moov 移到文件头，方便边下边播；保留原 metadata。",
    ],
  },
  {
    id: "crf",
    title: "为什么用 CRF，而不是固定码率",
    paragraphs: [
      "固定码率会强迫每一秒都花差不多的比特。运动剧烈或纹理复杂的片段会不够用，出现块效应；静止或平坦的片段又会浪费空间。这就是很多「在线压缩」看起来糊、有的地方却体积不小的原因。",
      "CRF 走相反的路：你指定质量目标，编码器自己决定这一帧需要多少比特。文件大小是结果，不是输入。近原优化的是观感下限，而不是保证压到某个 MB 数。",
      "如果源视频已经编码得很好，输出可能只小一点，甚至因为换封装而几乎不变——这是质量优先，不是失败。码率虚高的屏幕录像、相机直出或快速导出，收益通常非常明显。",
    ],
  },
  {
    id: "parameters",
    title: "各档参数各自负责什么",
    paragraphs: [
      "同一套 CRF 数字在 H.264、H.265 和 AV1 上不可直接对比。x265 与 SVT-AV1 通常更高效，所以近原给它们更高的 CRF，让三档编码的观感尽量接近。下面表格的数字来自当前预设，页面会与代码保持同步。",
    ],
    bullets: [
      "服务器速度：近无损 / 高画质的 H.264、H.265 用 slow 或 medium，用更长计算换更小文件。",
      "浏览器速度：同一 CRF，预设更快（medium / veryfast 等），避免 WASM 编码过久。",
      "pix_fmt=yuv420p：几乎所有播放器都能解码，避免 4:4:4 或少见位深导致手机无法播。",
      "tag:v=hvc1：让 Safari / QuickTime 认得出 HEVC。",
      "默认尽量复制音频；不能复制时，近无损 192 kbps AAC，往下各档递减。",
    ],
  },
  {
    id: "what-we-do-not-do",
    title: "近原默认不会做的事",
    paragraphs: [
      "很多「把视频变小」的工具其实在改内容。近原把这些排除在默认路径之外，因为它们会明显改变品质或语义。",
    ],
    bullets: [
      "默认不降低分辨率，也不抽帧、不裁切。",
      "不用固定低码率（例如全程 1 Mbps）去赌文件大小。",
      "不转成 GIF 或低色彩动图。",
      "不做伪「AI 超分」或锐化滤镜来掩盖压缩损伤。",
      "不把文件发到第三方转码平台。",
    ],
  },
  {
    id: "limits",
    title: "局限，以及什么时候不该用它",
    paragraphs: [
      "已经用 CRF 18 左右压过的 HEVC / AV1，再次接近无损重编码，收益通常很小。未压缩或高码率中间格式（ProRes、DNxHD、相机原片）收益会非常明显。",
      "多次有损重编码会累积代数损失。需要反复剪辑时，应保留一份高质量母带，只在最终分发时走近原。",
      "浏览器路径受内存限制，不适合很大的文件。服务器路径是单机 FFmpeg 进程，适合个人分享和自托管，不是海量并发的转码集群。",
    ],
  },
];
